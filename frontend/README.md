# CivicOS Frontend

React, TypeScript, and Vite build foundation for CivicOS.

```powershell
npm.cmd install
npm.cmd run lint
npm.cmd run test
npm.cmd run build
npm.cmd run dev
```

Role-specific routes and operational components are introduced from Phase 13 onward according to the master implementation specification.
